import request from "supertest";

test("/edithack route should not be accessible without jwt token", async done => {
  // const response = await request("http://localhost:3001/edithack")
  //   .post()
  //   .send({ data: "test" });
  // console.log(response);
  done();
});
